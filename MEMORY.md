# MEMORY — Kriya Portfólio

Fatos-chave do projeto (detalhes e narrativa completa em `HANDOFF.md`):

- Local canônico do código: `G:\Pedro\Dev\Kriya` | GitHub: https://github.com/pedroribeiro2706/kriya (público, conta pedroribeiro2706)
- Rodar: `npm run dev` → http://localhost:5173/versoes.html (estação de testes) ou /index.html (cópia de trabalho)
- `portfolio.html` = página principal publicada (export Webflow + GSAP inline no fim, ~linha 1379); desde 20/07 `index.html` é a CÓPIA DE TRABALHO dele com a seção da luminária (o protótipo antigo virou `index-old-02.html`)
- Seção da luminária (`index.html` + `css/luminaria.css`) — **COMPLETA e APROVADA em 26/07** (entrada, pisca, carrossel e saída; commits `ab2a9d3`..`fa18db1`). NÃO apagar arquivos de teste antigos
- **SITE NO AR** desde 26/07: https://kriya-pedroribeiro2706s-projects.vercel.app (Vercel, projeto `kriya`, deploy automático a cada push em `main`; site estático puro via `vercel.json`). `index.html` = página canônica; `portfolio.html` = legado
- **SAÍDA DA HERO RESOLVIDA e APROVADA em 27/07** (merge `2221d91`, no ar): o pin da hero foi estendido para 160vh no desktop e a animação de saída dos textos passou a rodar com a tela imóvel. Arquitetura: **três ScrollTriggers com uma responsabilidade cada** — `hero-pin` (só prende), `hero-video` (só o vídeo, `scrub: 3`), `hero-exit` (só a saída, `scrub: 1`). Faixas são posições absolutas derivadas de `vh()`, sem um trigger depender do outro. Calibráveis: `HERO_EXIT_VH = 0.6` e o scrub da saída
- **OVERLAP DOS TEXTOS DA DIREITA e SETA DE SCROLL RESOLVIDOS em 06/08** (merge `fc469e0`, no ar, deploy verificado por fetch da produção): máscara do `.hero-title-02-wrapper` alinhada à linha dos 74% (`bottom: 26vh` + `height: calc(215px - 6vh)`, topo invariante ⇒ layout parado intacto) — saída sem cruzamento; `transition` CSS da seta removida — GSAP manda sozinho em opacity/transform
- Backlog priorizado da hero: (1) suavidade do scrub do vídeo — ANTES de mexer, verificar se `scrub` em `ScrollTrigger.create()` sem animação anexada sequer suaviza o `self.progress` (suspeita de 27/07, nunca verificada); (2) responsividade mobile ("tudo muito desencaixado"; no mobile a luminária não existe — o acordeon `#service` assume). Detalhes e roteiro em HANDOFF 06/08
- Guias visuais em `references/`: `animacao-luminaria-ideal-01..05.png` = sequência-alvo da saída (montada pelo Pedro no Photoshop); `secao-luminaria-pos-fix-*.png` = registros de UAT
- Pasta antiga no OneDrive (`...\Kriya Design\Portfolio`) = arquivo morto de assets pesados (Photoshop/ e Videos/, ~10 GB, fora do git); sem .git; não editar código lá
- test-video-02-bkp-02.html ≡ test-video-02.html (idênticos); a variação está nos JS
- GSAP 3.13+ tornou ScrollSmoother/SplitText gratuitos; melhoria futura é migrar para gsap via npm
- Limpeza adiada (lista completa no HANDOFF.md, seção "Próximos passos")

## Log de sessões

- **2026-07-19** — Retomada: diagnóstico do projeto, git init + push inicial (`c2bd7d6`), clone para fora do OneDrive, estação de testes de versões (`c4def0a`), handoff criado. Pedro testou todas as versões na estação — conclusões ainda não comunicadas; pedir na próxima sessão.
- **2026-07-20** — Pedro comunicou as conclusões (`revisao.md`); setup do `index.html` como cópia de trabalho do `portfolio.html` (`index.html` antigo virou `index-old-02.html`). Brainstorming da seção da luminária → spec aprovada → plano de 7 tasks → implementação via subagentes (commits `ea7bc3d`..`3c7a913`). UAT do Pedro achou 2 bugs (switch invisível + menu quebrado), causa-raiz investigada e corrigida (`262287f`) — Pedro confirmou os 2 fixes funcionando ao vivo. UAT achou 1 bug novo (título da seção coberto pelo menu fixo) — não corrigido ainda. Sessão encerrando para reiniciar o Claude Code (troca Sonnet 5 → Fable 5, plano Max). Extensão do Chrome confirmada conectada. Detalhes completos no `HANDOFF.md`.
- **2026-07-20 (tarde/noite)** — Título coberto pelo menu corrigido (`5ee3611`, `top: 160px` fixo). Descoberto e corrigido bug crítico: vídeo da hero sem metadata no `window.load` lançava exceção não capturada que impedia a criação de TODOS os ScrollTriggers da luminária (`a319386`, guard `isFinite`). Lição registrada no handoff: aba controlada pela extensão do Chrome fica `hidden` e o Chrome throttla rAF — usar `gsap.ticker.tick()` / `.progress()` direto para verificar.
- **2026-07-21** — Recalibração do facho (`--lum-clip-on` medido da boca da cúpula) e do `.lum-rig` (âncora no rodapé, 82.2vh). Ficou no working tree, sem commit, até 23/07.
- **2026-07-23** — UAT aprovou a calibração de 21/07 (commitada como `8b54bed`) + entrada e pisca. Bug da SAÍDA analisado frame a frame (facho desconecta da cúpula): causa = interpolação vértice a vértice do clip-path com mapeamento invertido + janelas dessincronizadas. Fix intermediário `7772c6d` (clip-full reordenado, beam+rig na mesma janela 87–99) — melhorou, mas desconexões residuais são estruturais. **Decisão: luminária SVG articulada com o facho como path dentro do grupo da cúpula; saída em 2 fases (cúpula gira → rig desliza), guia em `references/animacao-luminaria-ideal-01..05.png`. Pedro está produzindo o SVG (spec no HANDOFF.md) — próxima sessão começa integrando o arquivo quando ele chegar.** Extensão do Chrome desconectada a sessão toda; verificação visual foi do Pedro via screenshots.
- **2026-07-26** — (1) Manhã: coreografia da saída da luminária FINALIZADA e aprovada após 3 rodadas de calibração com UAT (aprendizados: cadeia rígida — cada tween de "compensação" é ponto de descolamento; verificação de ângulos por pontos reais via getScreenCTM; clip-path não estende fundo de elemento). Commits `3fb7229`, `fa18db1`. (2) Tarde: DEPLOY — projeto `kriya` na Vercel conectado ao GitHub, push→deploy validado, Vercel Authentication desativada, site público; `vercel.json` força estático puro (NUNCA deixar autodetectar "Vite"); `index.html` virou a página canônica (CLAUDE.md atualizado). (3) Noite: tentativa de ajuste da saída da hero (pin 160vh + saída reversa com tela parada) reprovada no UAT real e revertida via `git restore` (nunca commitada; produção intocada). Motivo da reprovação ainda não relatado — próxima sessão COMEÇA pedindo esse relato antes de qualquer reimplementação.
- **2026-07-27** — Saída da hero RESOLVIDA e aprovada no UAT ("funcionou muito bem"), publicada em `2221d91`. Pedro descreveu o fluxo inteiro em detalhe e delimitou o pedido: a animação interna dos textos está correta, o defeito é a página rolar por baixo dela. Diagnóstico medido: a timeline `hero-exit` era a única ancorada fora de qualquer pin, scrubada nos 100vh de rolagem real entre o fim do vídeo e a chegada da luminária — deslocamento da `#hero` durante a saída era **−310px**, depois da correção **0px**. Solução: separar o trigger único (que prendia a tela E rodava o vídeo sob o mesmo `scrub: 3`) em três, com uma responsabilidade cada, e estender o pin para cobrir a saída — sem alterar um único valor da animação dos textos. **Lição-chave:** a tentativa reprovada de 26/07 mudou o mecanismo E a animação ao mesmo tempo; quando o usuário diz que a animação está quase certa e o problema é a sensação, mexer só no mecanismo. Fluxo completo brainstorming → spec → plano → execução com verificação no browser a cada etapa. Pendências no HANDOFF 27/07.
- **2026-08-06** — Itens 1 e 2 do backlog RESOLVIDOS, aprovados no UAT ("Ficou ótimo!") e publicados (merge `fc469e0`). (1) Overlap dos textos da direita: causa raiz medida era cruzamento MÚTUO — título desce e descrição sobe atravessando ~45px de faixa compartilhada entre as máscaras, ambos ~80% opacos; fix CSS-only de 2 declarações alinhando as fronteiras na linha dos 74% (topo do wrapper invariante por construção; JS e os seis `.to()` intocados — a lição de 27/07 aplicada de novo: mexer só no mecanismo). (2) Seta de scroll: `transition` CSS removida; sintoma comprovado antes (GSAP 0.1004 vs computed 1) e igualdade depois. Fluxo spec→plano→execução nos dois itens; verificação pilotada em 3 alturas de janela e 3 pontos de progresso. Descoberta operacional nova: `ScrollTrigger.refresh()` disparado com a página parada no meio da faixa e ticker congelado corrompe os starts dos tweens — protocolo de pilotagem corrigido e documentado no HANDOFF 06/08. Bônus: teardown do matchMedia <992px verificado ao vivo (resize acidental da janela). Nota: `log.md` não existe na raiz — o skill `registrar` pede os dois arquivos; registro feito só no MEMORY.md.
- **2026-07-25** — SVG entregue e integrado. Reestruturação (grupos aninhados base | braco > cupula > facho, pivôs nos eixos). Correção do Pedro sobre a saída: movimentos SOBREPOSTOS, não 2 fases (rig desliza desde o início; braço desce a haste verticalizando; leque ABRE por morph). Etapa 1 (troca estática 1:1, viewBox recortado replicando a caixa do PNG, cor #625e5e) — UAT APROVADO, commit `ab2a9d3`. Etapa 2 (luz = `#facho` estendido ~3x; `.lum-beam` = container transparente do conteúdo z4; fundo preto = `.lum-curtain` que desliza na saída; coreografia t85–100 com 5 tweens sobrepostos; abertura do leque por interpolação nativa do `d`, sem MorphSVG) — implementada e pré-validada por estados (0.5/0.88/0.93/0.96/1.0 ≈ frames ideal-01..05). Pedro saiu antes do UAT completo; **próxima sessão: ele vai explicar os ajustes que ainda quer.** Extensão do Chrome funcionou; workarounds de pilotagem documentados no HANDOFF.
